services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: energyworld
      POSTGRES_PASSWORD: energyworld
      POSTGRES_DB: energyworld
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
